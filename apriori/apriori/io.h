#include "define_header.h"

struct itemStructArray * get_dataset();
struct itemStruct link_transcation_of_item(struct itemStruct, int);

void show_all_itemArray(struct itemStructArray *);
void show_largeItem_level1(struct itemListStructArray *);
